package ${mapperTargetPackage};

import ${baseMapperPackage};
import ${entityPackage}.${entityName};

/**
 *
 * @author ${author} on ${createTime}
 */
public interface ${entityName}Mapper extends ${baseMapperName}<${entityName},${primary}> {
}
